/*console.log('helloworld')

for (let i=0; i < 5; i++){
        console.log (1)
    }

function oddNumber (){
    for (let i = 1; i <= 100; i++){
        if (i % 2 !==0) {
            console.log (i);
    }
}
}
oddNumber()

/*for (let i = 1; i <= 30; i++) {
    if (i % (3*5) == 0) console.log("FizzBuzz");
    else if (i % 3 == 0) console.log("Fizz");
    else if (i % 5 == 0) console.log("Buzz");
    else console.log(i);
}*/

/*const DAYS  = ['monday', 'tuesday', 'sunday']

DAYS[0] ='senin';
console.log(DAYS)*/

/* Fungsi untuk menghasilkan bilangan  min (inklusif) dan max (inklusif)
function getRandomInt(min, max) {
    min = Math.ceil(min);
    max = Math.floor(max);
    return Math.floor(Math.random() * (max - min + 1)) + min;
}


const numbers = [];
for (let i = 0; i < 10; i++) {
    numbers.push(getRandomInt(1, 100));
}

// Total, rata-rata, min, dan max
let total = 0;
let min = numbers[0];
let max = numbers[0];

for (let i = 0; i < numbers.length; i++) {
    total += numbers[i];
    if (numbers[i] < min) {
        min = numbers[i];
    }
    if (numbers[i] > max) {
        max = numbers[i];
    }
}

const average = total / numbers.length;

// Tampilkan hasil
console.log("Bilangan acak:", numbers);
console.log("Total:", total);
console.log("Rata-rata:", average);
console.log("Nilai minimum:", min);
console.log("Nilai maksimum:", max);
*/

